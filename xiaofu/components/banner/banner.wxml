<view class="banner-frame">
   <image src="{{imgSrc + src}}" mode="widthFix" class="banner-img" wx:if="{{src !== ''}}"></image>
   <view class="btn-confirm" hover-class="btn-confirm-hover" hover-stay-time="50" bindtap="gotoDetail" wx:if="{{type !== '4'}}">了解详情</view>
</view>
