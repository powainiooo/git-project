<view wx:if="{{locationData !== null}}">
   <map
       id="map"
       longitude="{{longitude}}"
       latitude="{{latitude}}"
       markers="{{markers}}"
       show-location
       style="width: 100%; height: 670rpx; margin-bottom: 40rpx;"
   >
      <cover-image src="../../res/index/guide.png" class="btn-guide" bindtap="doGuide"></cover-image>
   </map>
   <view class="map-info">
      <view class="name">{{locationData.mchName}}</view>
      <view class="distance">
         <text>距您{{locationData.minKM}}KM</text>
         <image src="../../res/index/icon-geo.png" mode="widthFix"></image>
      </view>
   </view>
   <view class="map-info">
      <view class="distance">营业时间：{{locationData.businessHours}}</view>
      <view class="working" wx:if="{{locationData.lineScene !== 0}}">
         <text>{{locationData.lineScene === 0 ? '' : '神笔画画在线中'}}</text>
      </view>
   </view>
   <view class="map-addr">{{locationData.location}}</view>
   <view class="btn">
      <z-button bindtap="getStore">刷新位置</z-button>
   </view>
   <swiper autoplay style="width: 100%; height: 423rpx" wx:if="{{locationData !== null}}">
      <block wx:for="{{storeUrl}}" wx:key="{{item}}">
         <swiper-item>
            <image src="{{item}}" class="slide-image" style="width: 100%;" mode="widthFix" />
         </swiper-item>
      </block>
   </swiper>
</view>
<view class="btn" wx:if="{{locationData === null}}">
   <z-button bindtap="getStore">刷新位置</z-button>
</view>
<image src="{{placeholderUrl}}" mode="widthFix" style="width: 100%;" wx:if="{{locationData === null}}"></image>


<i-message id="message" />