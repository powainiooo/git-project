<swiper autoplay style="height: 400rpx">
  <block wx:for="{{listData}}" wx:for-item="item" wx:key="{{item.id}}">
    <swiper-item>
      <image src="{{item.img}}" class="slide-image" mode="widthFix"/>
    </swiper-item>
  </block>
</swiper>