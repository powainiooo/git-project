<swiper circular style="height: 550rpx">
  <block wx:for="{{listData}}" wx:for-item="item" wx:key="id">
    <swiper-item data-src="{{item.url}}" bindtap="linkWeb">
      <image src="{{item.img}}" class="slide-image" mode="aspectFill"/>
    </swiper-item>
  </block>
</swiper>
