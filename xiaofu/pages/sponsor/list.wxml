<!--index.wxml-->
<view class="container">
  <header-view></header-view>
  <view class='body'>
    <view class='sponsor-list'>
      <navigator wx:for="{{dataList}}" 
      wx:for-index="index" 
      wx:for-item="item" 
      wx:key="{{index}}" 
      url="/pages/index/index?id={{item.mid}}"
      hover-class='none'>
        <image src="{{item.logo_img}}" mode='aspectFit'></image>
      </navigator>
    </view>
  </view>
</view>
